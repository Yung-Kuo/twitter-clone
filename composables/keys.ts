/**
 * Central `InjectionKey` symbols when migrating off string `inject('...')` (Phase 3d).
 * Example:
 * `export const useAlertKey: InjectionKey<ReturnType<typeof useAlert>> = Symbol('useAlert')`
 */
export {};
