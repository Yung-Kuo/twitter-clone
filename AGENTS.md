# Agent instructions (twitter-clone)

## Curriculum auto-build

This repo uses an **agent-driven** junior-to-senior refactor. Do not ask the user to manually run appendix `rg` checklists.

```bash
npm run curriculum:next    # next phase + prompt
npm run curriculum:verify  # automated exit criteria
```

Follow `.cursor/rules/curriculum-auto-build.mdc` and `docs/curriculum/AGENT_CONTRACT.md`.

Progress: `REFACTOR_STATUS.md`.
